# ── React / Node ──
node_modules/
build/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Firebase ──
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# ── Studio — outputs y archivos generados ──
studio/output/
studio/**/media/
studio/**/__pycache__/
studio/**/*.pyc

# ── Studio — credenciales y entorno (NUNCA subir) ──
studio/firebase-credentials.json
studio/.env

# ── Studio — entornos virtuales Python ──
studio/.venv/
studio/**/.venv/
studio/**/.venv310/

# ── Studio — clips de voz personal (privacidad) ──
studio/voz/referencias/

# ── Sistema ──
.DS_Store
Thumbs.db
*.log